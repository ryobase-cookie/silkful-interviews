# 編集ルール / Contributing Guide

このリポジトリを編集する前に、必ず一度目を通してください。

---

## 🚨 最重要：薬機法対応

このサイトで扱っている SILKFULL PRO は**動物用サプリメント**です。日本の**薬機法（医薬品医療機器等法）**に抵触する表現は使用しないでください。

### ❌ 使ってはいけない表現

| NG表現 | 理由 |
|---|---|
| 「○○に効く」「効果がある」 | 効能効果の断定は医薬品的 |
| 「○○を治す」「治療する」 | 治療の標榜は医薬品的 |
| 「○○を予防する」 | 予防効果の標榜は医薬品的 |
| 「成分○○が△△に作用する」 | 成分の薬理作用の説明はNG |
| 「BUN／クレアチニンが下がる」 | 検査値改善の標榜はNG |
| 「腎臓病が改善する」 | 疾患への効果はNG |

### ✅ OKな表現（言い換え例）

| 言いたいこと | OKな表現 |
|---|---|
| 効果がある | 「使っている」「採用している」「取り入れている」 |
| 治る | （使わない。代わりに）「飼い主から〜という声があった」 |
| 予防 | （使わない。代わりに）「健康管理に活用している」 |
| 成分の話 | 「多孔性の構造を活かして」など物性の話に留める |
| 飲ませやすい | 「嗜好性が良い」「テクスチャーが良い」 |

### ✅ ライティングの基本姿勢

- **「先生がこう使っている」「飼い主がこう言った」という"観察・体験の報告"** で書く
- **製品が何かをした、と書かない**。常に「使った人がどう感じたか」で書く
- 不安なときは管理者（三橋）に相談 → 確認を取ってから公開

---

## 📂 ファイル構成のルール

### 記事の置き場所

現在は `index.html` に草坂先生のインタビューが入っています。今後インタビューが増える場合：

```
/                           ← トップページ（一覧）
/kusaka/index.html          ← 草坂先生
/yamada/index.html          ← (例) 山田先生
/yamada/images/             ← 山田先生記事用の画像
```

…のような構成へ移行を検討します。先生が増えるタイミングで管理者に相談してください。

### 画像のルール

- **保存場所**: `images/` フォルダ
- **ファイル名**:
  - 写真をそのまま使う場合: `IMG_XXXX.jpg`（撮影時のままでOK）
  - 加工した画像: `kusaka-portrait-2026.jpg` のように **誰の・何の・いつ** がわかる名前
- **容量**: 1ファイル 5MB 以下を推奨（読み込み速度のため）
- **解像度**: 横幅 1600px 以下で十分
- **形式**: `.jpg`（写真）/ `.png`（ロゴ・図表）

### 命名規則

| 種類 | 命名例 |
|---|---|
| 先生の写真 | `kusaka-portrait.jpg` |
| 製品の写真 | `silkful-pro-cat.jpg` |
| 病院の外観 | `kusaka-hospital-exterior.jpg` |
| ロゴ・図表 | `silkful-logo.png` |

---

## 🛠 編集の進め方

### 編集のサイズ別おすすめツール

#### 🟢 小さな編集（誤字、文言修正）→ Web UI

1. https://github.com/kazuto32desu/silkful-interviews/blob/main/index.html
2. 右上の **鉛筆アイコン** をクリック
3. 編集
4. 下にスクロール → 「Commit changes」
5. **変更理由を1行書く**（例：「リード文の誤字修正」）
6. 「Commit changes」ボタン
7. **1〜2分後に本番反映**

#### 🟡 中規模編集（複数箇所、画像差し替え）→ GitHub Desktop

1. https://desktop.github.com/ からアプリDL
2. リポジトリをクローン
3. ローカルで好きなエディタ（VS Code, Cursor等）で編集
4. 編集中は `index.html` をブラウザで開いてプレビュー可能
5. GitHub Desktop で「Commit to main」→「Push origin」

#### 🔴 大規模編集（構成変更、新記事追加）→ ブランチ運用

```bash
git checkout -b feature/new-interview-yamada
# 編集
git add .
git commit -m "山田先生インタビュー記事を追加"
git push origin feature/new-interview-yamada
# GitHub上で Pull Request を作成
```

→ 三橋がレビュー → Merge → 本番反映

---

## 💬 コミットメッセージのルール

### 基本フォーマット

日本語でOK。**何を変えたか・なぜ変えたか** が分かれば十分。

### 良い例

- ✅ `リード文の誤字を修正`
- ✅ `草坂先生のポートレート写真を新しいものに差し替え`
- ✅ `編集後記の文末を「です・ます」調に統一`
- ✅ `タイトルを"〜という新しい引き出し"に変更（先生の意向反映）`

### 悪い例

- ❌ `更新` （何を更新したか不明）
- ❌ `修正` （何を修正したか不明）
- ❌ `aaa` （意味不明）

---

## ⚠️ 編集時の注意

### 1. 同時編集を避ける

2人が同じファイルを同時に編集すると **コンフリクト** が起きます。

**対策**:
- 編集前に Slack #silkful-interviews で「`index.html` を今から触ります」と宣言
- 5分以内に終わる小編集なら宣言不要

### 2. main ブランチに直接 commit する場合は慎重に

今の設定では、main に commit すると **即本番反映** されます。

- 公開前に内容を確認したい → ローカルでブラウザプレビュー
- 自信がない変更 → ブランチを切って Pull Request

### 3. 画像の容量

GitHub の単一ファイル上限は 100MB ですが、**5MB を超える画像は事前に圧縮** してください。
推奨ツール: [Squoosh](https://squoosh.app/)（ブラウザで無料・サインイン不要）

### 4. プライバシー・取材許諾

- インタビュー対象の先生のお名前・写真の掲載許諾を**必ず取得済み**にしてください
- 飼い主の個人情報（症例の詳細など）が映り込んでいないか確認してください

---

## 🔄 公開までのフロー（おさらい）

```
[編集] ローカル or Web UIで編集
   ↓
[Commit] 変更を確定
   ↓
[Push] GitHub に送る（Web UIなら自動）
   ↓
[自動デプロイ] GitHub Actions が走る（1〜2分）
   ↓
[公開] https://kazuto32desu.github.io/silkful-interviews/ に反映
```

デプロイの状況は https://github.com/kazuto32desu/silkful-interviews/actions で確認できます。

---

## 📞 困ったとき

- **公開URLが更新されない** → Actions タブでデプロイ失敗の有無を確認
- **コンフリクトが起きた** → Slack で三橋に連絡
- **薬機法の判断に迷う** → 必ず公開前に三橋に確認
- **GitHub の使い方がわからない** → Slack #silkful-interviews で質問

---

最終更新: 2026年4月
